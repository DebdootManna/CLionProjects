#include <stdio.h>

int main() {
    int No_of_Female = 10;
    int No_of_Male = 7;
    float ratio = (float)No_of_Female / No_of_Male;
    printf("The ratio of females to males is: %.2f\n", ratio);
    return 0;
}
